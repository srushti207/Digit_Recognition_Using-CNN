# HandWritten Digit_Recognition_Using-CNN

This project implements a Convolutional Neural Network (CNN) to recognize handwritten digits (0–9) using the MNIST dataset. The project includes loading the dataset, building the CNN model, training, and evaluating its accuracy.
